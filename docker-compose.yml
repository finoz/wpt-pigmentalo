services:
  wordpress:
    image: wordpress:latest
    depends_on:
      - db
    ports:
      - "${WP_PORT:-8080}:80"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: ${DB_USER:-wp}
      WORDPRESS_DB_PASSWORD: ${DB_PASSWORD:-wp}
      WORDPRESS_DB_NAME: ${DB_NAME:-pigmentalo}
      WORDPRESS_DEBUG: "1"
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_DEBUG_LOG', true);
        define('WP_DEBUG_DISPLAY', false);
        define('SCRIPT_DEBUG', true);
        define('WP_ENVIRONMENT_TYPE', 'local');
    volumes:
      - ./wp-content/themes/pigmentalo:/var/www/html/wp-content/themes/pigmentalo
      - ../wpt-ficus:/var/www/html/wp-content/themes/ficus
    networks:
      - pigmentalo

  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: ${DB_NAME:-pigmentalo}
      MYSQL_USER: ${DB_USER:-wp}
      MYSQL_PASSWORD: ${DB_PASSWORD:-wp}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - pigmentalo

  adminer:
    image: adminer
    depends_on:
      - db
    ports:
      - "${PMA_PORT:-8081}:8080"
    networks:
      - pigmentalo

volumes:
  db_data:

networks:
  pigmentalo:
